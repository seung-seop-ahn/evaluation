# {DATASET_NAME} 평가 결과 · {YYYY-MM-DD HH:mm}

실행: {claude-evaluate | langsmith-evaluate}
케이스 {N}건 × 반복 {k}회 · 채점 불가 {N}건
LangSmith 실험: {실험 이름}  (Experiments 탭에서 이 이름으로 찾으세요)

<!-- 방식 B(Claude 직접 수행)로 돌렸다면 반드시 남긴다 -->
<!-- 주의: 답변을 만든 쪽과 채점한 쪽이 같습니다. 관대해질 수 있는 조건이므로
     판정 근거 인용을 함께 보시길 권합니다 -->

## 1. 게이트 판정: {통과 | 미달}

| 기준 | 목표 | 실제 | |
|---|---|---|---|
| {기준} | {목표} | {실제} | {통과\|미달} |

<!-- 목적이 "약점 찾기"면 이 절을 지운다. 기준선이 없는 것이 정상 -->

## 2. 회귀 (직전 실행 대비)

**새로 실패한 케이스** — 총점이 그대로여도 여기 뭔가 있으면 뭔가 깨진 것입니다.

| id | 이전 | 이번 |
|---|---|---|
| {id} | 통과 | 실패 |

**새로 통과한 케이스**

| id | 이전 | 이번 |
|---|---|---|
| {id} | 실패 | 통과 |

<!-- 직전 리포트가 없으면: "직전 실행 기록이 없어 비교를 건너뜁니다" -->

## 3. 슬라이스별 통과율

전체 {N}% ({통과}/{전체}), 95% 신뢰구간 [{하한}%, {상한}%]

<!-- 구간 폭이 20%p를 넘으면 반드시 덧붙인다 -->
<!-- 케이스가 {N}개뿐이라 구간이 넓습니다. 이 수치로 몇 %p 차이를
     논하는 것은 의미가 없습니다 -->

**능력별**

| 능력 | 통과 |
|---|---|
| {capability} | {N}/{M} ({P}%) |

**상황별**

| 상황 | 통과 |
|---|---|
| {case_type} | {N}/{M} ({P}%) |

마이크로 평균 {N}% / 매크로 평균 {N}%

<!-- 두 값이 5%p 이상 벌어지면 덧붙인다 -->
<!-- 칸별 케이스 수가 달라 작은 칸의 실패가 전체 평균에서 희석되고 있습니다 -->

**tool 지표** (tools 데이터셋일 때만)

precision {N} / recall {N} / F1 {N}
트래젝토리 정확 일치 {N}
불필요 호출률 {N}

**거부·환각** (범위 밖 케이스가 있을 때만)

올바로 거부 {N}% · 근거 없이 지어냄 {N}%

## 4. 반복 실행 안정성 (k={k})

<!-- k=1이면 이 절을 통째로 지운다. 두 값이 같아 의미가 없다 -->

pass@{k} = {N}%  한 번이라도 통과
pass^{k} = {N}%  매번 통과

차이 나는 {N}건이 불안정한 케이스입니다. 통과로 집계되었지만 믿을 수 없습니다:
{id}, {id}

## 5. 실패 상세

### {id} · 실패 · 원인: {시스템 결함 | 케이스 오류 | 채점 기준 모호}

질문: {question}
기대: {answer}
실제: {actual}
판정 근거: {인용과 점수}
{k>1이면} {k}회 중 {N}회 실패

→ {후속 조치}

<!-- 원인별 후속 조치 문구
     시스템 결함  : 시스템에서 무엇을 고쳐야 하는지
     케이스 오류  : "케이스를 고칠 후보입니다. 승인해 주시면 needs_review로
                    내립니다" — 절대 직접 고치지 않는다
     채점 기준 모호: 루브릭을 어떻게 고칠지 제안 -->

## 6. 채점하지 못한 케이스

| id | 사유 |
|---|---|
| {id} | 근거 문서가 바뀌어 needs_review로 내려감 ({source_rev} → {현재}) |
| {id} | tool 호출이 필요한 케이스인데 방식 B로 실행됨 |

## 7. 다음 행동

**커버리지 공백**

| 능력 × 상황 | 목표 | 현재 |
|---|---|---|
| {capability} × {case_type} | {N} | {N} |

**승격 후보** — 이번 실행에서 관찰된 실패 중 케이스로 만들 만한 것

- {설명}

---

원본 출력은 `runs.csv` 에 있습니다. 리포트만으로 부족할 때 보세요.

**점수 100%는 목표가 아닙니다.** 만점은 이 골든셋이 이미 시스템에 맞춰졌다는
신호이고, 그때부터는 범위 밖의 문제를 경고해 주지 못합니다.
